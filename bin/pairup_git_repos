#!/bin/bash -e

list=$PAIRUP_GIT_REPOS
[[ -n "$1" ]] && list="${list}_$1"

if [[ -n "$list" ]]; then
    (
        mkdir -p ~/src
        cd ~/src
        for repo in $list; do
            git clone $repo || true
        done
    )
else
    echo "'$list' variable is not defined"
fi
