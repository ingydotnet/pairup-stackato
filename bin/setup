#!/bin/bash -xe

DOTDOTDOT_REPO=https://ingydotnet@github.com/ingydotnet/....git

if [[ -n "$DOTDOTDOT_REPO" ]]; then
    if [[ ! -d ~/... ]]; then
        git clone $DOTDOTDOT_REPO
        ./.../...
    fi
fi
