#!/bin/bash -xe

source /app/app/pairenv
echo $PAIR_USER
exit 1

export HOME=/app/fs/home
cd

if [[ -n $IN_PAIR ]]; then
    echo "Already inside bin/pair"
    exit 1
fi
export IN_PAIR=1

[[ ! -d "..." ]] && setup_pair_env

source .bashrc
exec bash

